base_global_planner: "waypoint_global_planner/WaypointGlobalPlanner"  # Global planner plugin to use
planner_frequency: 0.0  # Frequency at which the global planner will be called (Hz); 0.0 means to use the default value
planner_patience: 1.0  # Time the planner will wait in seconds for a valid plan before giving up
